# 删除回复空行

AstrBot 插件：在机器人消息发送前自动删除回复文本中的空行。(Codex编写)

## 功能

- 使用 `on_decorating_result` 钩子处理最终发送结果，不改 LLM 请求和上下文。
- 只处理 `Plain` 文本消息段，不影响图片、语音、文件、At 等组件。
- 默认将 2 个及以上连续换行压缩为 1 个换行。
- 正确处理含空白字符（空格、制表符）的空行。

例如：

```text
没关系的

我刚刚还以为自己真的漏掉了什么重要的事……
```

会变成：

```text
没关系的
我刚刚还以为自己真的漏掉了什么重要的事……
```

以下情况也会被正确处理（空行中含空格/制表符）：

```text
没关系的

我刚刚还以为自己真的漏掉了什么重要的事……
```

→

```text
没关系的
我刚刚还以为自己真的漏掉了什么重要的事……
```

## 配置

`max_consecutive_newlines` 控制最多保留多少个连续换行：

- `1`：删除空行，推荐默认值。
- `2`：最多保留一个空行。
- `0`：删除所有换行，不推荐，可能让多行文本挤成一行。

`apply_to_llm_output_globally` 控制是否默认对 LLM 原始输出全局生效：

- `true`：对任何最终输出统一处理。
- `false`：仅对常规聊天这类 LLM 原始输出处理，插件产出的结果默认不处理。

`plugin_blacklist` 用于指定插件黑名单：

- 无论全局模式是否开启，命中黑名单的插件输出都不会处理。
- 填 AstrBot 插件 ID，一行一个，例如 `astrbot_plugin_kfc_thursday`。

## 安装

将本目录放入 AstrBot 的插件目录后，在 AstrBot 管理面板启用插件即可。

## 更新日志

### v1.1.0

- **fix**: 修复正则 `\n{2,}` 无法匹配含空白字符空行的问题（如 `\n \n`、`\n\t\n`），这是空行清理不生效的主要原因
- **fix**: 修复 `max_consecutive_newlines=0` 时正则退化为删除所有换行的问题
- **chore**: 补充 `repo` 字段，修正版本号格式（去掉 `v` 前缀，遵循 PEP 440）
- **test**: 新增 `test_collapse_blank_lines.py` 测试文件，覆盖纯换行/含空白空行/CRLF/limit=0 等场景

### v1.0.0

- 初始版本
